<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link href="/css/style.css" rel="stylesheet" type="text/css">
</head>
<body>

  <div class="main-class">
    <p class="ingametxt"><%= npcname %></p>
      <p class="ingametxt">HP <%= npchealth %></p>
    <div class="npccontainer">
      <img class="npcimage" src=<%= npcfrontimage %> alt="" />
      <img class="npcfx" src=<%= fx %> alt="" />
    </div>
    <div class="usercontainer">
      <h1></h1>
      <img class="userfx" src=<%= npcfx %> alt="" />
      <img class="userimage" src=<%= backImage %> alt="" />
    </div>
    <p class="ingametxt"><%= name %></p>
      <p class="ingametxt">HP <%= health %></p>
    <% if (npchealth>0&&health>0) { %> 
    <button class="button"><a href='<%= battleUrl %>/<%= npchealth %>/30/<%= health %>/30'>ATTACK</a></button>
    <button class="button"><a href='<%= mypokemon %>'>RUN</a></button>
    <% } else if(npchealth<=0&&health<=0) { %>
      <h4>Both pokemon fainted!</h4>
      <button class="button"><a href='<%= mypokemon %>'>Go to profile</a></button>
    <% } else if(npchealth<=0) { %>
    <h4>CONGRATULATIONS! You defeated <%= npcname %></h4>
    <button class="button"><a href='<%= mypokemon %>/afterbattle'>Go to profile</a></button>
    <% } else if(health<=0) { %>
      <h4>TOO BAD! Your pokemon <%= name %> lost!</h4>
      <button class="button"><a href='<%= mypokemon %>'>Go to profile</a></button>
      <% } %>
  </div>
</body>
</html>